n = int(input())

for row in range(0, n):
    print('* '*n)

# TrimEnd means that it doesn't mean if there is space or no space in your code!
# In this case TrimEnd is used to remove the trailing characters from the string.